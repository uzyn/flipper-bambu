# fbt build artifacts
flipperzero-firmware/build/
flipperzero-firmware/dist/
flipperzero-firmware/toolchain/

# ufbt build artifacts
# `ufbt` writes only this file; the rest of .vscode/ stays trackable so shared
# editor settings are possible. (`ufbt vscode_dist` generates more, but nothing
# in this repo invokes it.)
.vscode/compile_commands.json

# Distribution output
dist/

# Test binary
test/test_bambu

# Local worktrees
.worktrees/
